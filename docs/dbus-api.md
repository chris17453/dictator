<!-- Generated by scripts/gen_docs.py. Do not edit by hand. -->

# D-Bus API

- **Bus name** `com.watkinslabs.Dictator1` (session bus)
- **Object path** `/com/watkinslabs/Dictator1`
- **Interface** `com.watkinslabs.Dictator1`

This is the product's real API. The `dictator` command is an ordinary
client with no privileged access, and any UI would be another one.

```bash
busctl --user introspect com.watkinslabs.Dictator1 /com/watkinslabs/Dictator1
```

## Methods

| Method | Arguments | Returns |
|---|---|---|
| `Cancel` | — | `'b'` |
| `GetHealth` | — | `'a{sv}'` |
| `GetMetrics` | — | `'s'` |
| `GetState` | — | `'a{sv}'` |
| `ListDevices` | — | `'a(ssbb)'` |
| `ListModels` | — | `'a(ssbbs)'` |
| `ListShortcuts` | — | `'a(sss)'` |
| `PushBegin` | `options: 'a{sv}'` | `'u'` |
| `PushEnd` | — | `'u'` |
| `Quit` | — | `''` |
| `Redeliver` | `entry_id: 'u', options: 'a{sv}'` | `'b'` |
| `Reload` | — | `'a{sv}'` |
| `Search` | `query: 's', limit: 'u'` | `'s'` |
| `SetDevice` | `name: 's'` | `'s'` |
| `SetModel` | `name: 's', options: 'a{sv}'` | `'a{sv}'` |
| `SetShortcut` | `shortcut_id: 's', chord: 's'` | `'a{sv}'` |
| `Toggle` | `options: 'a{sv}'` | `'(ub)'` |

## Signals

| Signal | Arguments |
|---|---|
| `Delivered` | `session_id: 'u', method_used: 's', app_id: 's', ok: 'b'` |
| `FaultOccurred` | `code: 's', message: 's', remedy: 's'` |
| `Final` | `session_id: 'u', text: 's', confidence: 'd'` |
| `Level` | `peak: 'd', rms: 'd', clipping: 'b', bands: 'ad'` |
| `Partial` | `session_id: 'u', text: 's', stable_chars: 'u'` |
| `StateChanged` | `state: 's', detail: 'a{sv}'` |

## Notes

- `Toggle` returns `(id, listening)`. The flag is what distinguishes
  starting from stopping; an id alone cannot.
- `Partial` carries `stable_chars`: that many leading characters have
  survived a re-decode and will not change. Render the rest as
  provisional, or the live view flickers.
- `Partial` is for display only. Delivery uses `Final`.
- `Level` fires at 50 Hz with peak, RMS, a clipping flag and eight
  log-spaced bands — enough for a VU meter, a waveform or a spectrum,
  from one signal.
- Arguments are bounded and validated: the bus is a trust boundary, and
  any process on it can call this.

